
/* import { createSlice } from "@reduxjs/toolkit";

const visiability = createSlice({
    name: 'visible',
    initialState: false,
    reducers: {
        setVisible: (_, action) => action.payload,
    }
});

export const { setVisible } = visiability.actions;

export const visibleReducer = visiability.reducer; */